// Cases de botões interativos para o switch de comandos do bot.
// Importa do módulo: import { sendInteractiveMessage, sendAlbumMessage } from './helpers/buttons.js';
//
// Convenção usada aqui:
// case '<nome>' -> versão SEM imagem (interactiveMessage cru, igual ao seu exemplo de 'innap')
// case '<nome>img'-> versão COM imagem (usa o formato "enhanced" do módulo: image + interactiveButtons,
//que faz o upload da foto e monta o header automaticamente)
//
// Cole os blocos abaixo dentro do seu switch (prefix, q, from, SoDono, reply, reagir, kiimorizinha
// já existem no seu handler, então não precisei redeclarar).

switch (command) {
// ── quick_reply (sem imagem) ──

case 'quickreply': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "quick_reply",
buttonParamsJson: "{\"display_text\":\"Confirmar\",\"id\":\"confirm_id\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── quick_reply (com imagem) ──

case 'quickreplyimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "quick_reply",
buttonParamsJson: "{\"display_text\":\"Confirmar\",\"id\":\"confirm_id\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_url (sem imagem) ──

case 'ctaurl': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "cta_url",
buttonParamsJson: "{\"display_text\":\"Visitar site\",\"url\":\"https://exemplo.com\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_url (com imagem) ──

case 'ctaurlimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "cta_url",
buttonParamsJson: "{\"display_text\":\"Visitar site\",\"url\":\"https://exemplo.com\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_copy (sem imagem) ──

case 'ctacopy': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "cta_copy",
buttonParamsJson: "{\"display_text\":\"Copiar código\",\"copy_code\":\"ABC123\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_copy (com imagem) ──

case 'ctacopyimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "cta_copy",
buttonParamsJson: "{\"display_text\":\"Copiar código\",\"copy_code\":\"ABC123\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_call (sem imagem) ──

case 'ctacall': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "cta_call",
buttonParamsJson: "{\"display_text\":\"Ligar agora\",\"phone_number\":\"5511999999999\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_call (com imagem) ──

case 'ctacallimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "cta_call",
buttonParamsJson: "{\"display_text\":\"Ligar agora\",\"phone_number\":\"5511999999999\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_reminder (sem imagem) ──

case 'ctareminder': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "cta_reminder",
buttonParamsJson: "{\"display_text\":\"Lembrar depois\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_reminder (com imagem) ──

case 'ctareminderimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "cta_reminder",
buttonParamsJson: "{\"display_text\":\"Lembrar depois\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_cancel_reminder (sem imagem) ──

case 'ctacancelreminder': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "cta_cancel_reminder",
buttonParamsJson: "{\"display_text\":\"Cancelar lembrete\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_cancel_reminder (com imagem) ──

case 'ctacancelreminderimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "cta_cancel_reminder",
buttonParamsJson: "{\"display_text\":\"Cancelar lembrete\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── address_message (sem imagem) ──

case 'addressmessage': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "address_message",
buttonParamsJson: "{\"display_text\":\"Enviar endereço\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── address_message (com imagem) ──

case 'addressmessageimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "address_message",
buttonParamsJson: "{\"display_text\":\"Enviar endereço\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── send_location (sem imagem) ──

case 'sendlocation': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "send_location",
buttonParamsJson: "{\"display_text\":\"Enviar localização\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── send_location (com imagem) ──

case 'sendlocationimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "send_location",
buttonParamsJson: "{\"display_text\":\"Enviar localização\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── open_webview (sem imagem) ──

case 'webview': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "open_webview",
buttonParamsJson: "{\"title\":\"Abrir site\",\"link\":{\"url\":\"https://exemplo.com\",\"in_app_webview\":true}}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── open_webview (com imagem) ──

case 'webviewimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "open_webview",
buttonParamsJson: "{\"title\":\"Abrir site\",\"link\":{\"url\":\"https://exemplo.com\",\"in_app_webview\":true}}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_catalog (sem imagem) ──

case 'ctacatalog': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "cta_catalog",
buttonParamsJson: "{\"business_phone_number\":\"5511999999999\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── cta_catalog (com imagem) ──

case 'ctacatalogimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "cta_catalog",
buttonParamsJson: "{\"business_phone_number\":\"5511999999999\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── mpm (sem imagem) ──

case 'mpm': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "mpm",
buttonParamsJson: "{\"product_id\":\"123456789012345\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── mpm (com imagem) ──

case 'mpmimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "mpm",
buttonParamsJson: "{\"product_id\":\"123456789012345\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── wa_payment_transaction_details (sem imagem) ──

case 'paymentdetails': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "wa_payment_transaction_details",
buttonParamsJson: "{\"transaction_id\":\"T-0001\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── wa_payment_transaction_details (com imagem) ──

case 'paymentdetailsimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "wa_payment_transaction_details",
buttonParamsJson: "{\"transaction_id\":\"T-0001\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── automated_greeting_message_view_catalog (sem imagem) ──

case 'greetingcatalog': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "automated_greeting_message_view_catalog",
buttonParamsJson: "{\"business_phone_number\":\"5511999999999\",\"catalog_product_id\":\"123456789012345\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── automated_greeting_message_view_catalog (com imagem) ──

case 'greetingcatalogimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "automated_greeting_message_view_catalog",
buttonParamsJson: "{\"business_phone_number\":\"5511999999999\",\"catalog_product_id\":\"123456789012345\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── galaxy_message (sem imagem) ──

case 'galaxy': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "galaxy_message",
buttonParamsJson: "{\"flow_token\":\"unused\",\"flow_id\":\"123456789012345\"}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── galaxy_message (com imagem) ──

case 'galaxyimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "galaxy_message",
buttonParamsJson: "{\"flow_token\":\"unused\",\"flow_id\":\"123456789012345\"}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── single_select (sem imagem) ──

case 'singleselect': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "single_select",
buttonParamsJson: "{\"title\":\"Selecione uma opção\",\"sections\":[{\"title\":\"Seção 1\",\"rows\":[{\"header\":\"Header\",\"title\":\"Opção 1\",\"description\":\"Descrição\",\"id\":\"opt1\"}]}]}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── single_select (com imagem) ──

case 'singleselectimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "single_select",
buttonParamsJson: "{\"title\":\"Selecione uma opção\",\"sections\":[{\"title\":\"Seção 1\",\"rows\":[{\"header\":\"Header\",\"title\":\"Opção 1\",\"description\":\"Descrição\",\"id\":\"opt1\"}]}]}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── inapp_signup (sem imagem) ──

case 'innap': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const msg = {
interactiveMessage: {
body: { text: q.trim() },
nativeFlowMessage: {
buttons: [{
name: "inapp_signup",
buttonParamsJson: "{}"
}],
messageParamsJson: "{}"
}
}
};
await sendInteractiveMessage(kiimorizinha, from, msg, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── inapp_signup (com imagem) ──

case 'innapimg': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: `oi`,
image: { url: "https://files.catbox.moe/mm2oki.jpg" },
interactiveButtons: [{
name: "inapp_signup",
buttonParamsJson: "{}"
}]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── carousel (sempre com imagem por card) ──
case 'carousel': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
const payload = {
text: 'Dá uma olhada nesses produtos 👇',
footer: 'Catálogo de exemplo',
carousel: [
{
image: { url: 'https://exemplo.com/produto1.jpg' },
title: 'Produto 1',
text: 'Descrição do produto 1',
buttons: [{ name: 'quick_reply', buttonParamsJson: JSON.stringify({ display_text: 'Comprar', id: 'buy_1' }) }]
},
{
image: { url: 'https://exemplo.com/produto2.jpg' },
title: 'Produto 2',
text: 'Descrição do produto 2',
buttons: [{ name: 'quick_reply', buttonParamsJson: JSON.stringify({ display_text: 'Comprar', id: 'buy_2' }) }]
}
]
};
await sendInteractiveMessage(kiimorizinha, from, payload, {});
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}

// ── album (várias fotos/vídeos em um AlbumMessage) ──
case 'album': {
if (!SoDono) return reply('❌ *Apenas o dono pode usar este comando!*');
try {
await sendAlbumMessage(kiimorizinha, from, [
{ type: 'image', image: { url: 'https://exemplo.com/foto1.jpg' }, caption: 'Foto 1' },
{ type: 'image', image: { url: 'https://exemplo.com/foto2.jpg' } },
{ type: 'video', video: { url: 'https://exemplo.com/video1.mp4' } }
], { delay: 800 });
await reagir(from, '✅');
} catch (error) {
await reply(`❌ *Erro:* ${error.message}`);
await reagir(from, '❌');
}
break;
}
}

case 'btn_location': {
if (!SoDono) return reply('❌ Apenas o dono pode usar este comando!')
try {
await sendInteractiveMessage(kiimorizinha, from, {
buttonsMessage: {
locationMessage: {
degreesLatitude: 0,
degreesLongitude: 0,
name: "Fatal - Frame",
address: "Online",
jpegThumbnail: Buffer.alloc(0)
},
contentText: `Olá @${sender.split('@')[0]}`,
footerText: `Teste do buttonsMessage\n\nBotão antigo do WhatsApp`,
buttons: [
{
buttonId: prefix + "menu",
buttonText: {
displayText: "☱ Menu"
},
type: 1
},
{
buttonId: prefix + "menup",
buttonText: {
displayText: "☱ Menu P"
},
type: 1
},
],
headerType: 6
}
}, {
quoted: selo,
mentions: [sender]
})
await reagir(from, "✅")
} catch (e) {
await ErroCase(e, prefix, command, NomeDoBot);
}
}
break
